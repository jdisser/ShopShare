export class Ingredient {
  name: string;
  unit: string;
  source: string;

  constructor(name: string, unit: string, source: string){
    this.name = name;
    this.unit = unit;
    this.source = source;
  }
}
